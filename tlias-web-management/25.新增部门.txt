一、需求分析
    1.需求
        通过点击新增部门按钮打开一个表单项，再输入新增的部门名称，插入这条数据。

    2.新增部门接口文档
        2.1 基本信息与请求参数
            如图
        2.2 响应数据
            如图


二、思路分析
    1.三层架构分别职责
        如图


三、controller接收参数——JSON格式参数
    1.什么是JSON格式参数
        接收json格式的请求参数：POST  /depts   {"name”：“教研部”}
        JSON格式的参数，通常会使用一个实体对象进行接收。

    2.接收规则：
        JSON数据的 键名 与方法形参 对象的属性名 相同，并需要使用 @RequestBody 注解标识。

    3.具体代码：
        如图


四、获取当前系统时间
    LocalDateTime.now()